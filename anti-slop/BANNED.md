# BANNED — 16 запретов, у каждого метод проверки

| № | Запрет | Метод | Паттерн / критерий |
|---|---|---|---|
| B-01 | transition: all — перечислять свойства явно | grep | `transition\s*:\s*all` |
| B-02 | Дефолтные easing: ease, ease-in-out, cubic-bezier(0.4,0,0.2,1), linear | grep | `ease-in-out | ease-in | \.4,\s*0,\s*\.2,\s*1` |
| B-03 | Hero «центрированный заголовок + подзаголовок + две кнопки» | арт-директор | `text-center + h1 + 2×btn` |
| B-04 | Три одинаковые карточки в ряд | арт-директор | `grid-cols-3 + 3×.card` |
| B-05 | Индиго/фиолетовые градиенты | grep | `#6366f1 | #8b5cf6 | #a855f7` |
| B-06 | Градиентный текст | grep | `background-clip\s*:\s*text` |
| B-07 | Глассморфизм как основной язык | grep + арт-директор | `backdrop-filter` |
| B-08 | Единственный шрифт Inter / системный без пары | арт-директор | `font-family: одна семья вне PAIRS` |
| B-09 | lorem ipsum в макете | grep | `lorem ipsum` |
| B-10 | Скругления 16px на всех элементах подряд | grep + арт-директор | `rounded-2xl | border-radius:16px` |
| B-11 | Эмодзи вместо графики в заголовках | grep | `<h1-3> + [\u{1F000}-\u{1FAFF}]` |
| B-12 | CTA-заглушки «Свяжитесь с нами», «Узнать больше» | grep | `узнать больше | свяжитесь с нами` |
| B-13 | blur-3xl / blur(80px+) — размытие в пыль | grep | `blur-3xl | blur(8\dpx)` |
| B-14 | «Нам доверяют» / «trusted by» без имён | grep | `нам доверяют | trusted by` |
| B-15 | Счётчик «500+ клиентов» без источника цифры | grep | `\d{3,}\+ клиент` |
| B-16 | Одинаковый ритм секций py-24 ×4 и более | grep | `py-24 повторяется ≥4` |
